accessing
nodes: anObject
	"The list of profile nodes. First item is the root node.
	type: array of:  $ref(ProfileNode)"
	<autoGeneratedMethod>

	nodes := anObject collect: [ :each |
		each isDictionary ifTrue:
			[ CDTPProfileNode fromDictionary: each client: client ]
		ifFalse:
			[ each ] ]
