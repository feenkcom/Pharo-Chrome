accessing
ranges: anObject
	"Source ranges inside the function with coverage data.
	type: array of:  $ref(CoverageRange)"
	<autoGeneratedMethod>

	ranges := anObject collect: [ :each |
		each isDictionary ifTrue:
			[ CDTPCoverageRange fromDictionary: each client: client ]
		ifFalse:
			[ each ] ]
