accessing
positions: anObject
	"	type: array of:  $ref(ScriptPosition)"
	<autoGeneratedMethod>

	positions := anObject collect: [ :each |
		each isDictionary ifTrue:
			[ CDTPScriptPosition fromDictionary: each client: client ]
		ifFalse:
			[ each ] ]
