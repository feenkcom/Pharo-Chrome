accessing
samples: anObject
	"	type: array of:  $ref(SamplingHeapProfileSample)"
	<autoGeneratedMethod>

	samples := anObject collect: [ :each |
		each isDictionary ifTrue:
			[ CDTPSamplingHeapProfileSample fromDictionary: each client: client ]
		ifFalse:
			[ each ] ]
