accessing
key: anObject
	"Preview of the key. Specified for map-like collection entries.
	Flags: optional"
	<autoGeneratedMethod>

	key := anObject isDictionary ifTrue:
		[ CDTPObjectPreview fromDictionary: anObject client: client ]
	ifFalse:
		[ anObject ]
