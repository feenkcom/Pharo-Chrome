accessing
functionLocation: anObject
	"Location in the source code.
	Flags: optional"
	<autoGeneratedMethod>

	functionLocation := anObject isDictionary ifTrue:
		[ CDTPLocation fromDictionary: anObject client: client ]
	ifFalse:
		[ anObject ]
