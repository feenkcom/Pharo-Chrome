accessing
scopeChain: anObject
	"Scope chain for this call frame.
	type: array of:  $ref(Scope)"
	<autoGeneratedMethod>

	scopeChain := anObject collect: [ :each |
		each isDictionary ifTrue:
			[ CDTPScope fromDictionary: each client: client ]
		ifFalse:
			[ each ] ]
