accessing
exception: anObject
	"Exception object if available.
	Flags: optional"
	<autoGeneratedMethod>

	exception := anObject isDictionary ifTrue:
		[ CDTPRemoteObject fromDictionary: anObject client: client ]
	ifFalse:
		[ anObject ]
