accessing
heapObjectId: anObject
	"Heap snapshot object id to be accessible by means of $x command line API."
	<autoGeneratedMethod>

	heapObjectId := anObject isDictionary ifTrue:
		[ CDTPHeapSnapshotObjectId fromDictionary: anObject client: client ]
	ifFalse:
		[ anObject ]
