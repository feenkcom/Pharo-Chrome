accessing
children: anObject
	"Child nodes.
	type: array of:  $ref(SamplingHeapProfileNode)"
	<autoGeneratedMethod>

	children := anObject collect: [ :each |
		each isDictionary ifTrue:
			[ CDTPSamplingHeapProfileNode fromDictionary: each client: client ]
		ifFalse:
			[ each ] ]
