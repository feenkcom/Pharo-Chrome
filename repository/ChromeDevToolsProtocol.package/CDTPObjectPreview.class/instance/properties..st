accessing
properties: anObject
	"List of the properties.
	type: array of:  $ref(PropertyPreview)"
	<autoGeneratedMethod>

	properties := anObject collect: [ :each |
		each isDictionary ifTrue:
			[ CDTPPropertyPreview fromDictionary: each client: client ]
		ifFalse:
			[ each ] ]
