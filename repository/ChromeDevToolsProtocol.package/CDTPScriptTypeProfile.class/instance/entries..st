accessing
entries: anObject
	"Type profile entries for parameters and return values of the functions in the script.
	type: array of:  $ref(TypeProfileEntry)"
	<autoGeneratedMethod>

	entries := anObject collect: [ :each |
		each isDictionary ifTrue:
			[ CDTPTypeProfileEntry fromDictionary: each client: client ]
		ifFalse:
			[ each ] ]
