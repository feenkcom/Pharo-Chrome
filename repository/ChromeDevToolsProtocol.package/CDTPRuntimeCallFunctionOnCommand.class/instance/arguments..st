accessing
arguments: anObject
	"Call arguments. All call arguments must belong to the same JavaScript world as the target
object.
	type: array of:  $ref(CallArgument)
	Flags: optional"
	<autoGeneratedMethod>

	arguments := anObject collect: [ :each |
		each isDictionary ifTrue:
			[ CDTPCallArgument fromDictionary: each client: client ]
		ifFalse:
			[ each ] ]
