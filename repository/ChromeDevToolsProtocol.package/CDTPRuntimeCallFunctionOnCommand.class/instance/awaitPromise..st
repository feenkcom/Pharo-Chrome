accessing
awaitPromise: anObject
	"Whether execution should `await` for resulting value and return once awaited promise is
resolved.
	type: boolean
	Flags: optional"
	<autoGeneratedMethod>

	awaitPromise := anObject
