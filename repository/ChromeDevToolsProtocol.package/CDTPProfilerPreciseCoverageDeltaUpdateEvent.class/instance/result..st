accessing
result: anObject
	"Coverage data for the current isolate.
	type: array of:  $ref(ScriptCoverage)"
	<autoGeneratedMethod>

	result := anObject collect: [ :each |
		each isDictionary ifTrue:
			[ CDTPScriptCoverage fromDictionary: each client: client ]
		ifFalse:
			[ each ] ]
