accessing
timeout: anObject
	"Terminate execution after timing out (number of milliseconds).
	Flags: experimental, optional"
	<autoGeneratedMethod>

	timeout := anObject isDictionary ifTrue:
		[ CDTPTimeDelta fromDictionary: anObject client: client ]
	ifFalse:
		[ anObject ]
