accessing
parentStackTraceId: anObject
	"Debugger will pause when async call with given stack trace is started."
	<autoGeneratedMethod>

	parentStackTraceId := anObject isDictionary ifTrue:
		[ CDTPStackTraceId fromDictionary: anObject client: client ]
	ifFalse:
		[ anObject ]
