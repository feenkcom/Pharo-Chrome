accessing
deoptReason: anObject
	"The reason of being not optimized. The function may be deoptimized or marked as don't
optimize.
	type: string
	Flags: optional"
	<autoGeneratedMethod>

	deoptReason := anObject
