accessing
skipList
	"The skipList specifies location ranges that should be skipped on step over.
	type: array of:  $ref(LocationRange)
	Flags: experimental, optional"
	<autoGeneratedMethod>

	^ skipList